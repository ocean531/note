#### 9.7 周一
今天主要是在看海康的官方文档，看一下怎么使用SDK，包括初始化SDK，初始化相机，取流操作，以及将SDK输出的格式转成opencv中的mat格式，这一部分的代码是ai写的，明天深入了解一下格式转换的算法，以及深入了解一下各个格式，目前代码编译成功，由于没有相机，我使用的是MVS软件自带的虚拟相机功能，在软件中可以正常使用，但是程序调用一次之后虚拟相机直接报废了，可以正常枚举端口但是无法打开设备，但是第一次调用的时候有画面。

将报错信息发给都报后得知是权限问题，虚拟相机还在被上一次的进程调用，我发现我都是之间ctrl加C结束程序，而没有让程序正常的跳出循环释放内存，现在有画面了，但是当我想要显示我导入的图片时，会黑屏。

MVSSDK中大量的使用了句柄，去专门的了解了一下句柄的概念以及与指针和引用的区别，句柄在调用大型的对象时会使用
#### 9.8周二
今天学习一下CMake的更多的使用方法，学习了一下set和文件搜索的操作

虚拟相机的图片显示还是有问题，显示成功了一次，但是后续就都是黑屏的情况，但是如果不传入图片的话可以正常显示默认的颜色


