#### 9.7 周一
今天主要是在看海康的官方文档，看一下怎么使用SDK，包括初始化SDK，初始化相机，取流操作，以及将SDK输出的格式转成opencv中的mat格式，格式转换这一部分的代码是ai写的，明天深入了解一下格式转换的算法，以及深入了解一下各个格式，目前代码编译成功，由于没有相机，我使用的是MVS软件自带的虚拟相机功能，在软件中可以正常使用，但是程序调用一次之后虚拟相机直接报废了，可以正常枚举端口但是无法打开设备，但是第一次调用的时候有画面。

将报错信息发给都报后得知是权限问题，虚拟相机还在被上一次的进程调用，我发现我都是之间ctrl加C结束程序，而没有让程序正常的跳出循环释放内存，现在有画面了，但是当我想要显示我导入的图片时，会黑屏。

MVSSDK中大量的使用了句柄，去专门的了解了一下句柄的概念以及与指针和引用的区别，句柄在调用大型的对象时会使用
#### 9.8周二
今天学习一下CMake的更多的使用方法，学习了一下set和文件搜索的操作[[cmake的使用#set()的使用]]
虚拟相机的图片显示还是有问题，显示成功了一次，但是后续就都是黑屏的情况，但是如果不传入图片的话可以正常显示默认的颜色，检查了一下并不是空帧，目前不知道是哪里有问题，使用真实摄像头之后，没调任何参数的情况下，画面比较黑而且图片分成了九个部分，上面三个帧率较高，下面六个很卡，后面发现是因为格式错了

现在可以稳定的调用画面，后面计划在SDK中尝试设置参数然后实现一下pnp算法

cs016uc输出的格式为PixelType_Gvsp_BayerRG8

学习了一下C++11的新特性：lambda表达式

#### 9.10周四
今天尝试一下serial库来通信，看了一下源代码，参考了一下晨舟的汇报
看到其中用到了pimpl的设计模式

#### 9.13周日
学习了一下cmake的变量操作，使用list进行字符串移除和拼接
写一下考核的题目
#### 9.14周一
了解一下socketcan
#### 9.15
了解了一下socketcan，使用虚拟CAN自己通信了一下，了解了一下socket的概念，和linux一切皆文件的思想，一切的io操作都可以看作是对文件进行读写，所以socketcan通过句柄来进行操作文件，句柄也就可以理解为是文件的编号

今天了解了一个工具socat。可以创建虚拟串口，使用程序互相发送，测试完自己的代码,和王瀚春哥调试了一下，成功发送了

接下来学习udev，定向串口

#### 9.16
今天尝试为USB串口固定设备名
由于没有硬件设备，我打算把我的键盘绑定一个名称，一开始按照博客来的时候，由于键盘和串口的规则可能不太一样，没有成功，后来使用SUBSYSTEM这个参数就可以了，令其等等USB就可以，

#### 9.18
今天补了一下编程基础，学习了一下C++11的新特性，静态断言，和左右值的概念和右值引用，以及移动语义和移动构造。












