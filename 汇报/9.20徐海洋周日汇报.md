看了3b1b的视频，学习了线性变换和矩阵的本质，之后继续学习，计划同步学习Eigen库的使用

梳理了一下同济25io层的结构，相机图像采集方面采用工厂模式加策略模式，将两种相机的使用封装在不同的类中，在使用时可以做到相互替换，使用camera这个工厂类，根据相机的种类来创建对应的对象，与c版的通信使用can总线，与云台和imu的通信使用串口（错误的），其实是两种方式可选

设置相机参数的函数也封装在构造函数里，构造函数接收从yaml文件中读取的参数，在内部完成设置

学习了11新特性右值引用，可以实现移动构造，直接转移内存的所有权，减少深拷贝开销
静态断言，在编译期就进行的断言







